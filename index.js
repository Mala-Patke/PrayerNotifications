const notify = require('./src/notify');
const parsePrayerTimes = require('./src/parsePrayerTimes');
const emitter = require('./events/newDay');

let prayertimes;
let 

function nextprayerTime(){

}

let lastprayer;

setInterval(() => {

})

setInterval(async () => {

    
}, 1000);

